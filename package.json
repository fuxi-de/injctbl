{
  "name": "injctbl",
  "version": "0.0.1",
  "license": "MIT",
  "private": false,
  "scripts": {
    "examples": "rm ./examples/bundle.js && cp ./dist/bundle.js ./examples && npx http-server ./examples",
    "build": "rm -rf dist && webpack",
    "dev": "rm -rf dist && webpack --watch"
  },
  "dependencies": {
    "@tiptap/core": "^2.0.0-beta.182",
    "@tiptap/starter-kit": "^2.0.0-beta.191"
  },
  "devDependencies": {
    "ts-loader": "^9.4.1",
    "typescript": "^4.7.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
